import styled from 'styled-components'

import { StyledMainPageWrapper } from '../../../common/styledComponents/styledWrappers'

export const StyledEmptyPackPage = styled(StyledMainPageWrapper)`
  .centerContainer {
    margin-top: 86px;
    text-align: center;
    H3 {
      margin-bottom: 32px;
    }
  }
`
