import styled from 'styled-components'

export const BackToPackArea = styled.div`
  position: absolute;
  display: flex;
  align-items: center;
  top: 84px;
  line-height: 24px;

  span {
    padding-left: 12px;
  }
  &:hover {
    cursor: pointer;
  }
`
