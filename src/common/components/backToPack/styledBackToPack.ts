import styled from 'styled-components'

export const BackToPackArea = styled.div`
  position: relative;
  display: flex;
  align-items: center;
  margin-top: 84px;
  line-height: 24px;

  span {
    padding-left: 12px;
    &:hover {
      cursor: pointer;
    }
  }
`
